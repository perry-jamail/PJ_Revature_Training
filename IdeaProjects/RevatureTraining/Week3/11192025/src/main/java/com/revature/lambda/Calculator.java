package com.revature.lambda;

public interface Calculator {
    // public int operation(int n1, int n2);

    // Write a lambda function that does not take a parameter and does not return a value, but simply prints "Hello".
    // public void helloPrint();

    // Write a lambda function that takes 1 String parameter and returns "Hello " + argument.toUpperCase()
    // public String helloUpperName(String name);

    // Write a lambda function that takes 2 String parameters, firstName and lastName, that returns "Hello " + firstName + lastName
    public String helloFullName(String firstName, String lastName);
}
